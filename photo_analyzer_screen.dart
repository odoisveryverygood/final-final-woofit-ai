import 'package:flutter/material.dart';

class PhotoAnalyzerScreen extends StatelessWidget {
  const PhotoAnalyzerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Photo Analyzer Screen")),
    );
  }
}
